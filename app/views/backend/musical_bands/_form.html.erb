<%= form_for(@musical_band, url: "/backend/musical_bands/#{@musical_band.id}" ) do |f| %>
  <% if @musical_band.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@musical_band.errors.count, "error") %> prohibited this musical_band from being saved:</h2>

      <ul>
      <% @musical_band.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  <div class="actions">
    <br>
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
